/**
 * [Holds the enviroment configuration like server url]
 * This value can be can replaced during build time using grunt or gulp
 */
(function() {
    window._envConfig = {
        uploadUrl: '/upload',
        baseUrl: 'http://localhost:3000'
    };
})();
